<Window x:Class="LiquorPOS.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LiquorPOS"
        xmlns:vm="clr-namespace:LiquorPOS.ViewModels"
        mc:Ignorable="d"
        Title="Liquor Store POS" Height="600" Width="900">

    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <Label Content="Scan Barcode:" VerticalAlignment="Center" FontWeight="Bold"/>
            <TextBox Width="300" Margin="10,0,0,0" FontSize="16"
                     Text="{Binding BarcodeText, UpdateSourceTrigger=PropertyChanged}">
                <TextBox.InputBindings>
                    <KeyBinding Key="Enter" Command="{Binding ScanBarcodeCommand}"/>
                </TextBox.InputBindings>
            </TextBox>
            <Button Content="Scan" Command="{Binding ScanBarcodeCommand}" Margin="10,0,0,0" Padding="10,2"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Margin="0,0,0,10">
            <TextBlock Text="Last Scanned Item:" FontWeight="Bold" FontSize="18" Margin="0,0,0,5"/>
            <TextBlock Text="{Binding FoundItemName}" FontSize="16" Margin="10,0,0,5" MinHeight="20"/>
            <TextBlock Text="{Binding FoundItemPrice}" FontSize="16" Margin="10,0,0,0" MinHeight="20"/>
        </StackPanel>

        <DataGrid Grid.Row="2" Margin="0,10,0,0"
                  ItemsSource="{Binding ScannedItemsList}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  HeadersVisibility="Column"
                  SelectionMode="Single"
                  FontSize="14">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Code" Binding="{Binding CodeNum}" Width="Auto"/>
                <DataGridTextColumn Header="Brand" Binding="{Binding Brand}" Width="1.5*"/>
                <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="2.5*"/>
                <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="Auto"/>
                <DataGridTextColumn Header="Size" Binding="{Binding Size}" Width="Auto"/>
                <DataGridTextColumn Header="Price" Binding="{Binding Price, StringFormat=C}" Width="Auto"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <TextBlock Text="Total:" FontWeight="Bold" FontSize="20" VerticalAlignment="Center"/>
            <TextBlock Text="{Binding TotalPrice, StringFormat=C}" MinWidth="120" TextAlignment="Right"
                       FontSize="20" Margin="10,0,0,0" VerticalAlignment="Center" FontWeight="Bold"/>
        </StackPanel>

    </Grid>
</Window>