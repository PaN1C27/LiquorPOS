<Window x:Class="LiquorPOS.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LiquorPOS"
        xmlns:vm="clr-namespace:LiquorPOS.ViewModels" mc:Ignorable="d"
        Title="Liquor Store POS" Height="450" Width="800">

    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>

    <Grid Margin="10">
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,20">
            <Label Content="Scan Barcode:" VerticalAlignment="Center" FontWeight="Bold"/>
            <TextBox Width="300" Height="20" Margin="10,0,0,0" FontSize="16"
                     Text="{Binding BarcodeText, UpdateSourceTrigger=PropertyChanged}">
                <TextBox.InputBindings>
                    <KeyBinding Key="Enter" Command="{Binding ScanBarcodeCommand}"/>
                </TextBox.InputBindings>
            </TextBox>
            <Button Content="Scan" Width="50" Height="20" Command="{Binding ScanBarcodeCommand}" Margin="10,0,0,0"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Margin="0,0,0,20">
            <TextBlock Text="Current Item:" FontWeight="Bold" FontSize="18" Margin="0,0,0,5"/>
            <TextBlock Text="{Binding FoundItemName}" FontSize="16" Margin="10,0,0,5"/>
            <TextBlock Text="{Binding FoundItemPrice}" FontSize="16" Margin="10,0,0,0"/>
        </StackPanel>
    </Grid>
</Window>